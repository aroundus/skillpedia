<!-- 이 문서는 원본입니다. -->

# 데이터 수집

`src/features/event-tracker`가 보내는 이벤트를 Google 태그 관리자와 Google 애널리틱스에서 받는 방법을 설명합니다. 추적 함수를 작성하는 방법은 코드를 참조하세요.

## 개요

Skillpedia는 태그 관리자 컨테이너를 통해 애널리틱스로 데이터를 보냅니다. 컨테이너는 `GTM_ID` 환경 변수에 값이 있고 production 모드로 빌드한 경우에만 페이지에 들어갑니다.

수집하는 값은 다음과 같습니다.

- 저장소 이름과 문서 경로
- 코드 블록의 언어
- 저장소 카드의 순위

다음은 수집하지 않습니다.

- 방문자를 식별하는 값
- 입력란에 적은 내용 중 저장소 형식을 통과하지 못한 값

## 수집 이벤트

| 이벤트                         | 전송 시점                                            | 매개변수                   |
| ------------------------------ | ---------------------------------------------------- | -------------------------- |
| `hero_submit_button_click`     | 히어로 입력 폼을 제출하고 저장소 형식 검사를 통과함  | `repository`, `is_example` |
| `repository_card_click`        | 저장소 둘러보기 카드를 클릭함                        | `repository`, `rank`       |
| `repository_card_impression`   | 저장소 카드가 화면에 절반 이상 1초 동안 머무름       | `repository`, `rank`       |
| `sidebar_document_link_click`  | 사이드바 또는 사이드 패널의 문서 항목을 클릭함       | `repository`, `path`       |
| `code_block_copy_button_click` | 문서 본문 코드 블록의 복사 버튼을 눌러 복사에 성공함 | `language`                 |

| 매개변수     | 값                                                       |
| ------------ | -------------------------------------------------------- |
| `repository` | `{owner}/{repo}` 형식의 저장소 이름                      |
| `path`       | 문서 경로. `/{owner}/{repo}`로 시작합니다                |
| `language`   | 코드 블록의 언어. 지정하지 않은 경우 `text`              |
| `rank`       | 저장소 카드의 순위. 순위 배지가 없는 카드는 `0`          |
| `is_example` | 입력 없이 제출해 안내 문구의 저장소로 이동한 경우 `true` |

- 값이 없는 매개변수는 전송하지 않으므로, 코드 복사 이벤트는 `language`만 보냅니다.
- 페이지 조회는 태그 관리자에 넣는 Google 태그가 자동으로 수집하므로 위 목록에 없습니다.

## 사전 준비

애널리틱스 속성과 태그 관리자 컨테이너를 만들고 다음 두 값을 받습니다.

| 값          | 형식            | 받는 곳                           |
| ----------- | --------------- | --------------------------------- |
| 측정 ID     | `G-`로 시작     | 애널리틱스 > 관리 > 데이터 스트림 |
| 컨테이너 ID | `GTM-`으로 시작 | 태그 관리자 > 작업공간 상단       |

애널리틱스가 설치 방법을 물으면 태그 관리자를 사용하므로 코드를 붙여넣지 않고 넘어갑니다.

Skillpedia는 클라이언트 라우팅으로 화면을 바꿉니다. 문서 사이 이동을 페이지 조회로 세려면 애널리틱스 > 관리 > 데이터 스트림 > 향상된 측정에서 '브라우저 기록 이벤트 기반 페이지 변경'을 켭니다.

## 태그 관리자 구성

### 변수

기본 제공 변수에서 `Event`를 켭니다. 태그가 이벤트 이름을 그대로 넘길 때 사용합니다.

사용자 정의 변수로 데이터 영역 변수 다섯 개를 만듭니다.

| 변수 유형        | 데이터 영역 변수 이름 |
| ---------------- | --------------------- |
| 데이터 영역 변수 | `repository`          |
| 데이터 영역 변수 | `rank`                |
| 데이터 영역 변수 | `path`                |
| 데이터 영역 변수 | `language`            |
| 데이터 영역 변수 | `is_example`          |

- '데이터 영역 변수 이름'은 코드가 보내는 키와 글자 그대로 같아야 합니다.
- '기본값 설정'을 체크하면 값이 없는 이벤트에도 그 값이 들어가므로 체크하지 않습니다.
- 측정 ID는 상수 변수로 만들어 태그에서 참조합니다.

### 트리거

맞춤 이벤트 트리거를 하나 만듭니다.

| 항목                      | 값                      |
| ------------------------- | ----------------------- |
| 트리거 유형               | 맞춤 이벤트             |
| 이벤트 이름               | `_(click\|impression)$` |
| 정규 표현식 일치 사용     | 체크                    |
| 이 트리거가 실행되는 시점 | 모든 맞춤 이벤트        |

- '정규 표현식 일치 사용'을 켜지 않으면 이벤트 이름 전체가 이 문자열과 같아야 해서 아무것도 걸리지 않습니다.

### 태그

태그 두 개를 만듭니다.

**Google 태그**

| 항목      | 값                          |
| --------- | --------------------------- |
| 태그 유형 | Google 태그                 |
| 태그 ID   | 측정 ID                     |
| 트리거    | 초기화 - 모든 초기화 이벤트 |

- 이 태그가 애널리틱스를 초기화하고 페이지 조회를 보냅니다.

**GA4 이벤트 태그**

| 항목        | 값                             |
| ----------- | ------------------------------ |
| 태그 유형   | Google 애널리틱스: GA4 이벤트  |
| 측정 ID     | 측정 ID                        |
| 이벤트 이름 | `{{Event}}`                    |
| 트리거      | 위에서 만든 맞춤 이벤트 트리거 |

이벤트 매개변수에 다섯 줄을 넣습니다.

| 이벤트 매개변수 | 값                    |
| --------------- | --------------------- |
| `repository`    | `{{repository 변수}}` |
| `rank`          | `{{rank 변수}}`       |
| `path`          | `{{path 변수}}`       |
| `language`      | `{{language 변수}}`   |
| `is_example`    | `{{is_example 변수}}` |

- 이벤트 이름에 `{{Event}}`를 넣으면 태그 하나가 이벤트를 모두 전달하므로, 이벤트마다 태그를 만들지 않아도 됩니다.
- 미리보기에서 홈의 카드를 클릭했을 때 이벤트 목록에 `repository_card_click`이 뜨고 GA4 이벤트 태그가 'Tags Fired'에 들어가면 정상입니다.
- 확인한 뒤 게시합니다.

## 애널리틱스 구성

매개변수는 등록해야 보고서에 나옵니다. 애널리틱스 > 관리 > 데이터 표시 > 맞춤 정의에서 만듭니다.

| 이벤트 매개변수 | 만들 곳       | 범위   |
| --------------- | ------------- | ------ |
| `repository`    | 맞춤 측정기준 | 이벤트 |
| `path`          | 맞춤 측정기준 | 이벤트 |
| `language`      | 맞춤 측정기준 | 이벤트 |
| `is_example`    | 맞춤 측정기준 | 이벤트 |
| `rank`          | 맞춤 측정항목 | 이벤트 |

- `is_example`은 참과 거짓을 문자열로 기록하므로 맞춤 측정기준으로 만듭니다.
- 등록 이전에 들어온 데이터에는 소급 적용하지 않습니다.

## 환경 변수

`GTM_ID`에 컨테이너 ID를 넣습니다.

```properties
GTM_ID=GTM-XXXXXXX
```

- 배포 환경에도 같은 값을 넣습니다. GitHub Actions로 배포하는 경우 저장소 설정의 Variables에 등록합니다.
- 값은 배포한 페이지 소스에 그대로 드러나므로 Secrets가 아닌 Variables를 사용합니다.
- `GTM_ID`를 넣어도 development 모드에서는 태그를 넣지 않으며, `npm run build`로 만드는 production 모드에서만 컨테이너를 불러옵니다.

## 이벤트 추가

트리거가 이벤트 이름의 접미사로 대상을 가리기 때문에, 이름을 `_click` 또는 `_impression`으로 끝내면 태그 관리자를 건드리지 않아도 새 이벤트가 수집됩니다.

```typescript
// src/features/event-tracker/lib/repository.tracker.ts

export const trackingTocLinkClick = (repository: string, path: string) => {
  trackingEvent('toc_link_click', { repository, path });
};
```

- 다른 접미사를 쓰는 경우 트리거의 정규 표현식을 함께 고쳐야 합니다.
- 고치지 않으면 이벤트가 데이터 영역에 쌓이기만 하고 애널리틱스로 넘어가지 않습니다.

새 매개변수를 보내는 경우에는 세 곳을 손봐야 합니다.

1. 태그 관리자에 데이터 영역 변수 추가
2. GA4 이벤트 태그에 이벤트 매개변수 행 추가
3. 애널리틱스에 맞춤 정의 등록

- 기존 매개변수로 답할 수 있는지 먼저 확인하면 이 작업을 줄일 수 있습니다.
- 무엇을 눌렀는지는 이벤트 이름이 담으므로, 위치를 매개변수로 나누기보다 이벤트 이름을 나누는 편이 간단합니다.
